# Play with git aliases

    $ git config --global alias.ci commit          # Add a git ci alias
    $ git config --global alias.last 'log -1 HEAD' # visualize last commit
    $ git config --get-regexp alias                # List your git aliases

